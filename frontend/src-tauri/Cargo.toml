[package]
name = "vostavo-desktop"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tauri = { version = "2" }

[build-dependencies]
base64 = "0.22"
tauri-build = { version = "2" }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
